<% if @feed_items.any? %>
  <ul>
    <% @feed_items.each do |post| %>
      <%= render "posts/post", post: post %>
    <% end %>
  </ul>
  <%= paginate @feed_items, theme: "twitter-bootstrap-4" %>
<% end %>
